import React from "react";

function Container() {
    return ( 
        <div style={newStyle} className="container">

        </div>

    );
}

const newStyle={
    width: "100%",
    maxWidth:"1300px",
    margin: "0 auto",
}

export default Container;